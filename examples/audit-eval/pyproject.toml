[project]
name = "audit-eval"
version = "0.1.0"
description = "Accounting AI evaluation harness for ExchangeAlgebra"
requires-python = ">=3.11"
dependencies = [
  "pandas>=2.0",
  "pytest>=8",
]

[tool.uv]
# gen/ uses pandas for its independent oracle; pytest is used by gen/selftests.
